[Unit]
Description=MPログ（日次のセルフトラッキング）の常駐サーバ
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/mp-log
ExecStart=%h/mp-log/.venv/bin/uvicorn main:app --host 127.0.0.1 --port 8000
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
