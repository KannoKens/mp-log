# -*- coding: utf-8 -*-
"""個人設定のサンプル。`cp config.example.py config.py` で複製し、自分用に編集する。

config.py は .gitignore 済み（公開リポに個人の語彙を載せないため）。
config.py が無くても db.py 側の汎用デフォルトで動く。
"""

# 入力フォームに固定タグとして並ぶ活動語彙。自分の生活に合わせて書き換える。
ACTIVITY_TAGS = ["仕事", "勉強", "運動", "家事", "趣味", "外出"]

# タグ → 消費MP（1〜5）。省略可（未設定なら空扱い）。
# 別の環境でやった活動を、入力済みのタグから activities に起こすときに引く表。
# db.fill_from_tags() が source='table' で書き込む。
#
# ⚠ 消費量が「読めない」活動は載せないこと。固定値を置くと、実態とズレたまま
#   /today の判断材料になる。分からないものは実測で入れる。
TAG_MP_COSTS = {
    "仕事": 5,
    "外出": 3,
    "家事": 2,
    "勉強": 1,
}
